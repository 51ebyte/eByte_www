#### 设置用户备注名
```java
/**
 * 设置用户备注名
 * 
 * @param openid 用户标识
 * @param remark 新的备注名，长度必须小于30字符
 * @return
 * @throws Exception
 */
officialAccount.user.updateremark(String openid, String remark)
```
